import React, { useState,useEffect,useMemo,useRef } from 'react';
import './style.css';

interface SliderProps {
    slides: string[];
  }
  
const SecondSlider: React.FC<SliderProps> = () => {
    return (
        <div>
            123
        </div>
    )
}

export default SecondSlider
